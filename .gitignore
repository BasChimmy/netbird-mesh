# NetBird setup keys and generated secrets
netbird/.keys/
*.key

# Self-signed CA + certs (regenerated locally)
netbird/certs/*.pem
netbird/certs/*.key
netbird/certs/*.crt
netbird/certs/*.srl

# NetBird control-plane runtime state
netbird/management.json
netbird/data/
netbird/store/
netbird/*.log

# Rendered manifests (contain injected credentials)
poc/k8s-statefulset/k8s/mariadb/.rendered/
poc/k8s-statefulset/k8s/netbird-router/.rendered/
netbird/config.yaml
netbird/dashboard.env

# Local env overrides
.env
*.local.env

# OS noise
.DS_Store

# Staged CA copy for docker build
poc/k8s-statefulset/devops-server/rootCA.pem
poc/bastion-server/bastion/rootCA.pem
poc/bastion-server/devops-server/rootCA.pem
